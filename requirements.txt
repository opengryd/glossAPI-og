### GlossAPI runtime requirements (aligned with repro_rapidocr_onnx)

# Core pipeline deps
pandas>=1.3.0,<3.0.0
numpy<2
python-dateutil>=2.8.2
pytz>=2021.1
scikit-learn==1.6.1
joblib>=1.0.0
dask>=2022.1.0
pyarrow>=7.0.0
aiohttp>=3.8.0
aiofiles>=23.0.0
ftfy>=6.0.0
tenacity>=8.0.0
tqdm>=4.67.0
questionary>=2.0.1
rich>=13.7.1
typer>=0.12.3

# Docling + RapidOCR ONNX stack
docling==2.48.0
# Prefer RapidOCR core package; it works with the GPU ORT wheel without pulling
# the CPU-only 'onnxruntime' dependency.
rapidocr>=3.3.0
onnxruntime-gpu==1.18.1
pyyaml>=6.0

# Enrichment & JSON compression (required for Phase-2 math/code and JSON zstd)
pypdfium2>=4.0.0
zstandard>=0.22.0
transformers>=4.45,<5

# Optional: install Torch CUDA for GPU layout (not required for OCR)
# pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.5.1 torchvision==0.20.1

# Optional: macOS MPS/Metal support for Phase-2 math
# pip install torch>=2.2,<3 torchvision>=0.17,<1
