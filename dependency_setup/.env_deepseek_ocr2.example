# DeepSeek-OCR v2 (MPS / MLX — macOS Apple Silicon) environment
#
# Copy this file to .env_deepseek_ocr2 and adjust the paths for your machine:
#   cp dependency_setup/.env_deepseek_ocr2.example dependency_setup/.env_deepseek_ocr2
# Then source it before running the pipeline:
#   source dependency_setup/.env_deepseek_ocr2

# ---------------------------------------------------------------------------
# Shared weights root
# ---------------------------------------------------------------------------
export GLOSSAPI_WEIGHTS_ROOT=/path/to/glossAPI/model_weights

# ---------------------------------------------------------------------------
# Python binary inside the DeepSeek-OCR v2 venv.
# ---------------------------------------------------------------------------
export GLOSSAPI_DEEPSEEK2_PYTHON=/path/to/glossAPI/dependency_setup/.venvs/deepseek-ocr-2-py312/bin/python

# ---------------------------------------------------------------------------
# CLI control
# ---------------------------------------------------------------------------
export GLOSSAPI_DEEPSEEK2_ENABLE_OCR=1
export GLOSSAPI_DEEPSEEK2_ENABLE_STUB=0

# ---------------------------------------------------------------------------
# Device for inference: mps (default on macOS Apple Silicon).
# ---------------------------------------------------------------------------
export GLOSSAPI_DEEPSEEK2_DEVICE="mps"

# ---------------------------------------------------------------------------
# MLX model weights directory (default: GLOSSAPI_WEIGHTS_ROOT/deepseek-ocr-mlx/).
# ---------------------------------------------------------------------------
# export GLOSSAPI_DEEPSEEK2_MODEL_DIR=/path/to/glossAPI/model_weights/deepseek-ocr-mlx

# ---------------------------------------------------------------------------
# Path to MLX inference script (default: package-embedded mlx_cli.py).
# ---------------------------------------------------------------------------
# export GLOSSAPI_DEEPSEEK2_MLX_SCRIPT=/path/to/glossAPI/src/glossapi/ocr/deepseek_ocr2/mlx_cli.py

# ---------------------------------------------------------------------------
# Misc
# ---------------------------------------------------------------------------
export GLOSSAPI_SKIP_DOCLING_BOOT=1
