# DeepSeek OCR v2 (MLX / macOS MPS) environment — generated by setup_glossapi.sh
#
# Copy this file to .env_deepseek_ocr2 and adjust the paths for your machine:
#   cp .env_deepseek_ocr2.example .env_deepseek_ocr2
# Then source it before running the pipeline:
#   source dependency_setup/.env_deepseek_ocr2

# Root directory for all model weights (shared across backends).
export GLOSSAPI_WEIGHTS_ROOT=/path/to/glossAPI/model_weights

# Python binary inside the DeepSeek OCR v2 venv.
export GLOSSAPI_DEEPSEEK2_PYTHON=/path/to/glossAPI/dependency_setup/.venvs/deepseek-ocr-2-py312/bin/python

# Disable stub backend (set to 1 only for tests / local dev).
export GLOSSAPI_DEEPSEEK2_ALLOW_STUB=0

# Enable the real MLX CLI runner.
export GLOSSAPI_DEEPSEEK2_ALLOW_CLI=1

# Device for inference: mps (default on macOS Apple Silicon).
export GLOSSAPI_DEEPSEEK2_DEVICE="mps"
