# OlmOCR-2 environment — generated by setup_glossapi.sh
#
# Copy this file to .env_olmocr and adjust the paths for your machine:
#   cp .env_olmocr.example .env_olmocr
# Then source it before running the pipeline:
#   source dependency_setup/.env_olmocr

# Root directory for all model weights (shared across backends).
export GLOSSAPI_WEIGHTS_ROOT=/path/to/glossAPI/model_weights

# Python binary inside the OlmOCR venv.
export GLOSSAPI_OLMOCR_PYTHON=/path/to/glossAPI/dependency_setup/.venvs/olmocr-py312/bin/python

# Disable stub backend (set to 1 only for tests / local dev).
export GLOSSAPI_OLMOCR_ALLOW_STUB=0

# Enable the real OlmOCR pipeline (CUDA only; omit on macOS/MPS).
# export GLOSSAPI_OLMOCR_ALLOW_CLI=1

# Device for inference: mps (macOS Apple Silicon) or cuda (Linux GPU).
export GLOSSAPI_OLMOCR_DEVICE="mps"
